<%= form_with(model: fan_comment) do |form| %>
  <% if fan_comment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(fan_comment.errors.count, "error") %> prohibited this fan_comment from being saved:</h2>

      <ul>
        <% fan_comment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :author_no, style: "display: block" %>
    <%= form.number_field :author_no %>
  </div>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :body, style: "display: block" %>
    <%= form.text_area :body %>
  </div>

  <div>
    <%= form.label :deleted, style: "display: block" %>
    <%= form.check_box :deleted %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
